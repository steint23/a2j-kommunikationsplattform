describe("config.client", () => {
  it("init", async () => {
    expect(true).toBe(true);
  });

  it("returns  a client config item as requested (SENTRY_DSN)", async () => {
    expect(true).toBe(true);
  });
});
